export function buildDeck(rows,{status='',domain='',cefr=''}={}){let deck=(Array.isArray(rows)?rows:[]).filter(r=>String(r?.c??'').trim());if(status)deck=deck.filter(r=>String(r.s??r.status??'Chưa học')===status);if(domain)deck=deck.filter(r=>Array.isArray(r.domains)&&r.domains.includes(domain));if(cefr)deck=deck.filter(r=>String(r.cefr||'')===cefr);for(let i=deck.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[deck[i],deck[j]]=[deck[j],deck[i]]}return deck}
export function normalizeSwipe(dx,threshold=70){if(dx>=threshold)return'learned';if(dx<=-threshold)return'not-learned';return''}
