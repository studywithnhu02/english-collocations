import {COACH_KEY,loadCoach,saveCoach,shouldRemind,coachModel} from './coach-core.mjs';import {ANALYTICS_KEY,localDateKey} from './analytics-core.mjs';import {GOALS_KEY} from './goals-core.mjs';
function read(key,fallback){try{return JSON.parse(localStorage.getItem(key)||'null')??fallback}catch{return fallback}}
function show(){const now=new Date(),a=read(ANALYTICS_KEY,{checkins:[],learningEvents:[]}),today=localDateKey(now),coach=loadCoach();if(!shouldRemind(now,a)||coach.lastShown===today)return;const events=Array.isArray(a.learningEvents)?a.learningEvents:[],count=new Set(events.filter(e=>e?.date===today).map(e=>String(e.id))).size,target=Number(read(GOALS_KEY,{day:5}).day||5),m=coachModel(now,a,target,Math.max(0,target-count));const box=document.createElement('div');box.id='coachToast';box.innerHTML='<div class="coach-box"><div class="coach-title">🤖 AI Coach</div><div class="coach-text">Hôm nay bạn chưa học collocation nào.<br>Còn <b>'+m.remaining+'/'+m.goalTarget+'</b> để hoàn thành mục tiêu hôm nay và giữ Streak <b>'+m.streak+' ngày</b>.</div><div class="coach-actions"><button type="button" id="coachQuiz">🎮 Ôn tập 1 phút</button><button type="button" class="secondary" id="coachDismiss">Để sau</button></div></div>';document.body.appendChild(box);const dismiss=()=>{saveCoach({...coach,lastShown:today});box.remove()};box.querySelector('#coachDismiss').onclick=dismiss;box.querySelector('#coachQuiz').onclick=()=>{dismiss();window.PreviewQuiz?.start?.()}}
function boot(){if(document.getElementById('coachStyles'))return;const s=document.createElement('style');s.id='coachStyles';s.textContent='#coachToast{position:fixed;right:18px;bottom:18px;z-index:100008}.coach-box{width:min(350px,calc(100vw - 36px));background:var(--card);border:1px solid var(--line);border-radius:15px;padding:13px;box-shadow:0 18px 50px #0007}.coach-title{font-weight:900;font-size:13px}.coach-text{font-size:10px;color:var(--muted);line-height:1.5;margin-top:6px}.coach-actions{display:flex;gap:6px;margin-top:9px}.coach-actions button{font-size:10px;flex:1;padding:7px 8px}';document.head.appendChild(s);show();setInterval(show,60000);window.addEventListener('preview-data-updated',show)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
window.PreviewCoach={refresh:show};
